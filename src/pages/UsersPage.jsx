import ResourcePage from './ResourcePage'
import * as api from '../api/modules/usersApi'
import { enums } from '../constants/enums'

const createFields = [{ name: 'employeeId', label: 'Employee ID', required: true }, { name: 'firstName', label: 'First name', required: true }, { name: 'lastName', label: 'Last name', required: true }, { name: 'email', label: 'Email', type: 'email', required: true }, { name: 'phoneNumber', label: 'Phone number' }, { name: 'password', label: 'Password', type: 'password', required: true }, { name: 'role', label: 'Role', options: enums.role, required: true }, { name: 'teamId', label: 'Team ID', type: 'number' }]
const updateFields = [{ name: 'firstName', label: 'First name', required: true }, { name: 'lastName', label: 'Last name', required: true }, { name: 'email', label: 'Email', type: 'email', required: true }, { name: 'phoneNumber', label: 'Phone number' }, { name: 'active', label: 'Active', type: 'boolean', options: ['true', 'false'] }, { name: 'roleId', label: 'Role ID', type: 'number' }, { name: 'teamId', label: 'Team ID', type: 'number' }, { name: 'managerId', label: 'Manager', userSelect: true }]
export default function UsersPage() { return <ResourcePage module="users" title="Users" description="Manage workforce access and team assignments." api={{ ...api, list: api.getAllUsers, create: api.createUser, update: api.updateUser, remove: api.deleteUser, sortBy: 'userId' }} idKey="userId" createFields={createFields} updateFields={updateFields} columns={[{ key: 'userId', label: 'ID' }, { key: 'employeeId', label: 'Employee ID' }, { key: 'firstName', label: 'Name', render: (row) => `${row.firstName} ${row.lastName}` }, { key: 'email', label: 'Email' }, { key: 'role', label: 'Role' }, { key: 'teamName', label: 'Team' }]} emptyMessage="No users found." /> }