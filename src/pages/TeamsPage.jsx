import ResourcePage from './ResourcePage'
import * as api from '../api/modules/teamsApi'
const fields = [{ name: 'teamName', label: 'Team name', required: true }, { name: 'teamLeadId', label: 'Team lead ID', type: 'number', required: true }]
export default function TeamsPage() { return <ResourcePage title="Teams" description="Keep delivery teams and their leads current." api={{ list: api.getAllTeams, create: api.createTeam, update: api.updateTeam, remove: api.deleteTeam, sortBy: 'teamId' }} idKey="teamId" createFields={fields} updateFields={fields} columns={[{ key: 'teamId', label: 'ID' }, { key: 'teamName', label: 'Team name' }, { key: 'teamLeadId', label: 'Lead ID' }, { key: 'teamLeadName', label: 'Team lead' }]} emptyMessage="No teams found." /> }